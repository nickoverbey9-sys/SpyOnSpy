node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

# Runtime bot state (kill switch, paper/positions, adoption ledger) — never commit
.data/

# Secrets — never commit
.env
.env.*
!.env.example
creds.txt
*.pem
*.key
*_token
*_token.txt

*.log
.DS_Store
